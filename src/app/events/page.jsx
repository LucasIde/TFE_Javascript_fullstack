import EventDislay from "@/components/eventMainpage/eventDisplay";

export default function eventsPage() {

    return (
        <main>
            <h1>events pages</h1>
            <EventDislay/>
        </main>
    );
}